
{
  "name": "run-tasov-migrations",
  "version": "1.0.0",
  "description": "Database migrations for Run Tasov application",
  "main": "run-migrations.js",
  "scripts": {
    "migrate": "node run-migrations.js",
    "setup": "npm install && npm run migrate"
  },
  "dependencies": {
    "mysql2": "^3.6.0"
  }
}
